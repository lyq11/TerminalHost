<Application x:Class="TerminalHost.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <!-- Keep native WPF popups consistent with the application theme. -->
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#475569" />
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#F8FAFC" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#475569" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#F8FAFC" />
        <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="#0F172A" />
        <SolidColorBrush x:Key="{x:Static SystemColors.MenuBarBrushKey}" Color="#0F172A" />
        <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="#E2E8F0" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#155E75" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#F8FAFC" />
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#475569" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="Padding" Value="9,6" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#64748B" /></Trigger>
                <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#155E75" /><Setter Property="Foreground" Value="#FFFFFF" /></Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
